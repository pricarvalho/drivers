# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                                       controllers.Application.index
PUT     /                                       controllers.Application.restart

#Cabby
POST    /cabbies                                controllers.Cabbies.post
GET     /cabbies/request-from-passenger/:uuid   controllers.Cabbies.requests(uuid)
POST    /cabbies/moves                          controllers.Cabbies.move

#Passenger
POST     /passengers                            controllers.Passengers.post

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file                           controllers.Assets.at(path="/public", file)

    